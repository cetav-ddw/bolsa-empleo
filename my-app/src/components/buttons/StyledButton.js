import styled from 'styled-components';

const Button = styled.button `
    align-items: center;
    background: rgba(244, 110, 37, 0.95);
    border-radius: 10px;
    border: none;
    box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
    display: flex;
    height: 60px;
    justify-content: space-between;
    width: 343px;
`;
export default Button;